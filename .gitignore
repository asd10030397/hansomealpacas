# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
/public/scale-preview
.tmp-trust-assets/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# hardhat
/contracts/node_modules
/contracts/cache
/contracts/artifacts
/contracts/typechain-types
/contracts/coverage
/contracts/.env

# market snapshot history (built automatically in production)
/data/market-history.json
.env*
# .env.example files are safe templates (no secrets) and should be
# committed — without this exception, the broad `.env*` rule above also
# blocks them.
!.env.example
!**/.env.example

# telegram buy bot (standalone module — see bot/README.md)
/bot/node_modules
/bot/dist
/bot/data
/bot/*.tsbuildinfo
