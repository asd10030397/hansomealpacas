"use client";

/**
 * @deprecated Use GameChrome (or DesktopGameNav / MobileGameHeader) instead.
 * Kept as a thin alias so older imports keep working during the chrome split.
 */
export { GameChrome as GameNav } from "./GameChrome";
