import { LaunchEndCard } from "@/components/game/LaunchEndCard";

/** Fullscreen launch end card — for trailer capture / marketing close. */
export default function LaunchEndPage() {
  return <LaunchEndCard fullscreen />;
}
